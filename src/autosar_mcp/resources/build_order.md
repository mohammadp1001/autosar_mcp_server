create_workspace

create_package_map

Create base types

Create impl data types

Create port interfaces + data elements

Create constants

Create SWCs + ports

Create internal behavior + runnables + events + port api options

Save as multiple ARXML files (platform/portinterfaces/constants/components)